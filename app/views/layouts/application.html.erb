<!DOCTYPE html>
<html>
<head>
  <title>Parks and Reviews</title>
  <link href="https://fonts.googleapis.com/css?family=Cutive+Mono|Six+Caps|Special+Elite&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="//cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="top-bar">
    <h1><a href="/">Parks and Reviews</a></h1>

    <section class="top-bar-section">
      <div class="right">
        <%- if user_signed_in? -%>
        <img class="thumbnail" src=<%= current_user.profile_photo %>>
          <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
          <% if current_user.admin? %>
            <br />
            <%= link_to "New Park", new_park_path %>
          <% end %>
        <%- else -%>
          <%= link_to 'Sign Up', new_user_registration_path %>
          <br>
          <%= link_to 'Sign In', new_user_session_path %>
        <%- end -%>
      </div>
    </section>
  </nav>

  <%- flash.each do |key, value| -%>
    <div class="flash flash-<%= key %>">
      <%= value %>
    </div>
  <%- end -%>

  <%= yield %>

  <%= javascript_include_tag 'application' %>

  <%- if Rails.env.development? || Rails.env.test? -%>
    <%= javascript_tag do %>
      window.railsEnv = '<%= Rails.env %>'
    <%- end -%>
  <%- end -%>
  <%= javascript_pack_tag 'application' %>

  <%= yield :extra_footer %>
</body>
</html>
